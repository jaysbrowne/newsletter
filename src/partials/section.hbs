<div
  class="{{classnames 'section' footer=footer openings=jobs }}"
  {{#if hideable}} mc:hideable{{/if}}
  {{#if variant}} mc:repeatable="body" mc:variant="{{variant}}"{{/if}}
>
  {{> @partial-block}}
</div>
